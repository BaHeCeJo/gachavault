# auth_enabled: false is intentional — Loki only listens on the internal
# Docker network (no `ports:` mapping in docker-compose.prod.yml), so the
# port 3100 is unreachable from outside the host. Authentication is
# enforced at the Grafana layer (which is the only consumer). Do NOT
# expose this port via `ports:` or nginx without flipping this to true.
auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9095

common:
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

# 30-day retention keeps storage bounded on a small VPS. Bump if your incident
# investigations regularly reach further back; shrink if disk is tight.
limits_config:
  retention_period: 720h
  reject_old_samples: true
  reject_old_samples_max_age: 168h
  allow_structured_metadata: true

compactor:
  working_directory: /loki/compactor
  retention_enabled: true
  delete_request_store: filesystem

analytics:
  reporting_enabled: false
